post:
  tags:
    - read
  summary: Returns information for provided sigmarket(s).
  description: Returns information for provided sigmarket(s).
  requestBody:
    required: true
    content:
      application/json:
        examples:
        schema:
          $ref: ..\components\schemas\SigmarketAsk.yaml
  responses:
    "200":
      description: Sigmarket(s) info
      headers:
        x-rate-limit-remaining:
          schema:
            type: integer
            description: Request limit per hour.
    "401":
      $ref: ..\components\responses\UnauthorizedError.yaml
